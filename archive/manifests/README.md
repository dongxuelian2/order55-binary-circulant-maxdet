# Archive manifests

`order15_mu3_manifest.json` is deliberately small and stable.  It records the
research question, exact lower bound, replay state, and authoritative paths;
it is not a generated hash manifest for every transient file.
